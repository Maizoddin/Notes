# application.properties

# Database Configuration
spring.datasource.url=jdbc:mysql://localhost:3306/user_management
spring.datasource.username=maaz
spring.datasource.password=Maaz@9060

# Hibernate Configuration
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.show-sql=true

# Logging Configuration
logging.file.name=logs/app.log
logging.level.org.hibernate.SQL=DEBUG

# For Hibernate 6.x (Parameter Binding)
logging.level.org.hibernate.engine.jdbc.bind.internal.BasicBinder=TRACE

# Remove older Hibernate 5.x binder (conflicts with Hibernate 6.x)
# logging.level.org.hibernate.type.descriptor.sql.BasicBinder=TRACE